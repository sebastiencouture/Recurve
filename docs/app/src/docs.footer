
    var container = recurve.container([
        recurve.flux.$module,
        recurve.flux.react.$module,
        docsModule]);

    container.invoke(["$document", "$stateRouter", "$State"], function($document, $stateRouter, $State) {
        React.render(React.createElement($State, null), $document.body);
        $stateRouter.start();
    });

})(window, recurve);