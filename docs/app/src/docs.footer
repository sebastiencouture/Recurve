
    var container = recurve.container([
        recurve.flux.$module,
        recurve.flux.state.$module,
        docsModule]);

    container.invoke(["$document", "App"], function($document, App) {
        React.render(React.createElement(App, null), $document.body);
    });

})(window, recurve);