/** @jsx React.DOM */

"use strict";

docsModule.factory("ApiView", null, function() {

    return React.createClass({
        render: function() {
            return (
                <div>api</div>
            );
        }
    });
});